Example Rails 3.2 application that uses http://www.passaporteweb.com.br as it's login / SSO solution.

See https://github.com/myfreecomm/omni_auth_myfc_id for the gem it uses.

To run the app, have Sqlite installed and:

rake db:setup
rails s
